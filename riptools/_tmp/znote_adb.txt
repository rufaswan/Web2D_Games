*ubuntu*
	mtp-detect

	adb keygen $HOME/.android/adbkey
	adb kill-server
	adb devices

*android*
	Setting -> About Phone -> Build number
		tap to enable Developer
	enable USB debugging

*ubuntu*
	adb shell
		pm list packages | grep -i TERM

		# uninstall system apps
		pm uninstall -k --user 0 APK

		# re-install system apps
		pm dump APK | grep -i system/app/
		pm install --user 0 system/app/PATH/APK

		pm install-existing APK

		pm disable APK

	logcat
